# Root lombok config — applies to the whole project.
# Prevents lombok from searching parent directories for additional configs.
config.stopBubbling = true

# Allow @Qualifier on fields to be copied onto the generated constructor parameter
# so that @RequiredArgsConstructor + @Qualifier("...") works for Spring bean disambiguation.
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
